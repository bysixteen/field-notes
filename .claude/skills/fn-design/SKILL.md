---
name: fn-design
description: Alias of fn-design-md. The canonical path is `.claude/skills/fn-design-md/SKILL.md` — open that file for the full skill.
---

# fn-design — alias

This skill is an alias for `fn-design-md`. The canonical path and all referenceable content live at:

  `.claude/skills/fn-design-md/SKILL.md`

If you reached this file from a memory entry, a note, or external documentation referring to "fn-design", follow the canonical path above. Both names point at the same skill; the alias exists to absorb the recurring "is `fn-design` the right path?" confusion.

The "-md" suffix in the canonical name reflects what the skill produces: a `DESIGN.md` file in the Google design.md spec, plus a passthrough DTCG `tokens.json` sidecar.

This is an alias for `fn-design-md`. The SELF-REVIEW protocol runs via delegation to the canonical skill; this stub does not invoke it independently.
