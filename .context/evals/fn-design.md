# eval: fn-design

Alias of `fn-design-md`. See [`fn-design-md.md`](./fn-design-md.md) for the full report.

The alias absorbs the recurring "is `fn-design` the right path?" confusion; both names point at the same skill, so they share the same score.
