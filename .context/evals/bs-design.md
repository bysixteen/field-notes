# eval: bs-design

Alias of `bs-design-md`. See [`bs-design-md.md`](./bs-design-md.md) for the full report.

The alias absorbs the recurring "is `bs-design` the right path?" confusion; both names point at the same skill, so they share the same score.
